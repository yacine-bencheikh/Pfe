const {Reservation} = require("../models/models");

module.exports ={
    makeReservation: async (req,res)=>{
        try {
            
        } catch (error) {
            console.log(error);
        }
    }
    
}